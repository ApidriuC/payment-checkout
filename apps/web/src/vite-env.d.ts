/// <reference types="vite/client" />

// Injected by Vite at build time and by jest.setup.ts when running the tests,
// so the source never has to read import.meta directly.
declare const __API_BASE_URL__: string;
